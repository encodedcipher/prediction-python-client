Google Prediction API Python Client

Author:  'hancock.robert@gmail.com (Bob Hancock)'

prediction.py
    This abstracts the REST calls to the API
    
examples
    fetch_auth.py
        Fetch the authorization token using your Google account.
        
    train_model.py
        Using your authorization token, train the model using 
        your bucket and data in Google Storage.  It validates
        the token and the verifies the existence of the resources
        in Google Storage.
        
TODO
    -   Handle captcha
    -   Can we pipeline the entire process with coroutines?
    -   Auth: def fetch_auth()
            If it exists in .boto use that
            else: new one and write to .boto
            .   keyword arg persist=True|False to write to .boto
            .   keyword to new=True|False - get new auth and use that.